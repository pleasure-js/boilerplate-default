module.exports = {
  debug: true{{#if config.ui}},
  ui: {
    postCssVariables: {
      theme: {
        profile: {
          // todo: see if it can be reloaded
          background: `black`, // can be accessed via var(--theme-profile-background) in any postcss scope
        }
      }
    }
  }{{/if}}
}
